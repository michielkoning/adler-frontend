#import "./Fragments/ImageArchive.gql"

query getArrangements($first: Int, $language: LanguageCodeFilterEnum) {
  arrangementsHighlights: arrangements(first: $first, where: { language: $language }) {
    edges {
      node {
        id
        title
        excerpt
        relativeUrl
        featuredImage {
          ...imageArchive
        }
        pricesGroup {
          priceFrom
        }
      }
    }
  }
}
