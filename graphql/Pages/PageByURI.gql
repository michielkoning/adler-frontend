#import "./Fragments/PageDetails.gql"

query PageByURI($uri: ID!) {
  page(id: $uri, idType: URI) {
    ...pageDetails
    translations {
      link
      uri
      slug
      relativeUrl
      language {
        code
        slug
      }
    }
  }
}
