#import "./../Fragments/Images/ImageArchive.gql"
#import "./Fragments/Page.gql"

query($after: String, $parent: String, $language: LanguageCodeFilterEnum) {
  pages(after: $after, where: { parent: $parent, language: $language }) {
    edges {
      node {
        ...page
        excerpt
        relativeUrl
        featuredImage {
          ...imageArchive
        }
      }
    }
  }
}
